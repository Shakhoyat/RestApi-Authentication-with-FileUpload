<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile Page</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.7/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-LN+7fdVzj6u52u30Kp6M/trliBMCMKTyK833zpbD+pXdCLuTusPj697FH4R/5mcr" crossorigin="anonymous">

    <style>
        body {
            background: linear-gradient(135deg, #1d63a4 0%, #3a8dde 100%);
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        .profile-container {
            background: #fff;
            border-radius: 18px;
            box-shadow: 0 8px 32px rgba(30, 60, 120, 0.18);
            padding: 48px 36px 36px 36px;
            max-width: 420px;
            width: 100%;
            position: relative;
        }

        .profile-avatar {
            width: 120px;
            height: 120px;
            object-fit: cover;
            border-radius: 50%;
            box-shadow: 0 4px 16px rgba(29, 99, 164, 0.12);
            border: 4px solid #f4f8fb;
            margin-bottom: 18px;
        }

        .profile-title {
            font-size: 2.1rem;
            font-weight: 700;
            color: #1d63a4;
            margin-bottom: 8px;
        }

        .profile-email {
            color: #6c757d;
            font-size: 1.05rem;
            margin-bottom: 18px;
        }

        .profile-divider {
            border-top: 2px solid #e3eaf3;
            margin: 28px 0 18px 0;
        }

        .btn-primary {
            background-color: #1d63a4;
            border: none;
            border-radius: 8px;
            font-weight: 600;
            padding: 10px 0;
            transition: background 0.2s;
        }

        .btn-primary:hover {
            background-color: #155084;
        }

        .profile-actions {
            margin-top: 24px;
        }

        .edit-profile-link {
            display: inline-block;
            margin-bottom: 12px;
            color: #1d63a4;
            font-weight: 500;
            text-decoration: none;
            transition: color 0.2s;
        }

        .edit-profile-link:hover {
            color: #155084;
            text-decoration: underline;
        }

        @media (max-width: 576px) {
            .profile-container {
                padding: 28px 8px 18px 8px;
            }
        }
    </style>
</head>

<body>
    <div class="profile-container text-center">
        <img src="<%= user.imgUrl %>" alt="Profile Picture" class="profile-avatar">
        <h2 class="profile-title">Welcome, <%= user.name %>!</h2>
        <div class="profile-email">
            <%= user.email %>
        </div>
        <div class="profile-divider"></div>
        <div class="profile-actions">
            <a href="/edit-profile" class="edit-profile-link">Edit Profile</a>
            <a href="/logout" class="btn btn-primary w-100">Logout</a>
        </div>
    </div>
</body>

</html>