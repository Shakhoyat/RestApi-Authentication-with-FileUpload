<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Register-Page</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.7/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-LN+7fdVzj6u52u30Kp6M/trliBMCMKTyK833zpbD+pXdCLuTusPj697FH4R/5mcr" crossorigin="anonymous">

    <style>
        body {
            background: linear-gradient(135deg, #1d63a4 0%, #3a8dde 100%);
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        .container {
            background: #fff;
            border-radius: 16px;
            box-shadow: 0 8px 32px rgba(30, 60, 120, 0.15);
            padding: 40px 32px 32px 32px;
            max-width: 420px;
            width: 100%;
        }

        .form-title {
            font-size: 2rem;
            font-weight: 700;
            color: #1d63a4;
            margin-bottom: 24px;
            text-align: center;
        }

        .form-control {
            margin-bottom: 18px;
            border-radius: 8px;
            border: 1px solid #d1e3f6;
            transition: border-color 0.2s;
        }

        .form-control:focus {
            border-color: #1d63a4;
            box-shadow: 0 0 0 2px rgba(29, 99, 164, 0.1);
        }

        .btn-primary {
            background-color: #1d63a4;
            border: none;
            border-radius: 8px;
            font-weight: 600;
            padding: 10px 0;
            transition: background 0.2s;
        }

        .btn-primary:hover {
            background-color: #155084;
        }

        .form-text {
            color: #6c757d;
            font-size: 0.95rem;
        }

        @media (max-width: 576px) {
            .container {
                padding: 24px 10px 18px 10px;
            }
        }
    </style>
</head>

<body>
    <div class="container">
        <h1 class="form-title">Create Your Account</h1>
        <form action="/register" method="POST" enctype="multipart/form-data" autocomplete="on">
            <div class="mb-3">
                <label for="username" class="form-label">Full Name</label>
                <input type="text" class="form-control" id="username" name="username" placeholder="Enter your full name"
                    required autofocus>
            </div>
            <div class="mb-3">
                <label for="email" class="form-label">Email Address</label>
                <input type="email" class="form-control" id="email" name="email" placeholder="you@example.com" required>
            </div>
            <div class="mb-3">
                <label for="password" class="form-label">Password</label>
                <input type="password" class="form-control" id="password" name="password"
                    placeholder="Create a password" minlength="6" required>
                <div class="form-text">Password must be at least 6 characters.</div>
            </div>
            <div class="mb-3">
                <label for="profile" class="form-label">Profile Picture</label>
                <input type="file" class="form-control" id="profile" name="file" accept="image/*" required>
                <div class="form-text">Accepted formats: JPG, PNG, GIF.</div>
            </div>
            <div class="d-grid gap-2 mt-4">
                <button type="submit" class="btn btn-primary btn-lg">Register</button>
            </div>
            <div class="text-center mt-3">
                <span class="form-text">Already have an account? <a href="/login" class="text-primary">Login</a></span>
            </div>
        </form>
    </div>
</body>

<body>

</body>

</html>